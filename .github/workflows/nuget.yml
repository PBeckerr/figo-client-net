name: dotnet core - build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GETDATE: date +%Y%m%d%H%M%S,
      GETDATE2: $(date)
    steps:
      - run: "echo $GETDATE2 ${{env.GETDATE2}}."
      - run: "sleep 5"
      - run: "echo $GETDATE2 ${{env.GETDATE2}}."
